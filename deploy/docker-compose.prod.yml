# Production-only additions layered on top of the base compose.yaml.
# Local dev deliberately doesn't run these as services - you start
# `sail artisan horizon` / `reverb:start` manually in a spare terminal only
# when you need them. Production needs them running continuously with
# auto-restart, which these three extra services provide (using Docker's
# own restart policy for supervision - simpler than adding supervisord
# inside the image for just three commands).
#
# Usage (from the project root, on the server):
#   docker compose -f compose.yaml -f deploy/docker-compose.prod.yml up -d
services:
    laravel.test:
        restart: unless-stopped

    horizon:
        image: sail-8.5/app
        restart: unless-stopped
        volumes:
            - '.:/var/www/html'
        networks:
            - sail
        # Without this, start-container's `exec gosu $WWWUSER "$@"` runs
        # with WWWUSER empty (only compose.yaml's laravel.test service sets
        # it) - bash then drops the empty argument entirely, shifting the
        # command's own first word ("php") into gosu's username slot, which
        # crash-loops with "unable to find user php: no matching entries in
        # passwd file". Matches the WWWUSER wiring compose.yaml already has
        # for laravel.test.
        environment:
            WWWUSER: '${WWWUSER}'
            LARAVEL_SAIL: 1
        depends_on:
            - pgsql
            - redis
        command: php artisan horizon

    reverb:
        image: sail-8.5/app
        restart: unless-stopped
        volumes:
            - '.:/var/www/html'
        networks:
            - sail
        environment: # see horizon's comment above on why this is required
            WWWUSER: '${WWWUSER}'
            LARAVEL_SAIL: 1
        depends_on:
            - pgsql
            - redis
        command: php artisan reverb:start

    scheduler:
        image: sail-8.5/app
        restart: unless-stopped
        volumes:
            - '.:/var/www/html'
        networks:
            - sail
        environment: # see horizon's comment above on why this is required
            WWWUSER: '${WWWUSER}'
            LARAVEL_SAIL: 1
        depends_on:
            - pgsql
            - redis
        # Laravel's scheduler is designed to be invoked once a minute (e.g.
        # via cron) and exit, not to run as a long-lived process itself -
        # this loop is the equivalent of that cron entry for a container
        # that has no host crontab to hook into.
        command: sh -c "while true; do php artisan schedule:run; sleep 60; done"
